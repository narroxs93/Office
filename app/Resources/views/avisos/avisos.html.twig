{% extends 'layout.html.twig' %}

{% block title %}Avisos{% endblock %}
{% block navbar %}
    <nav class="navbar navbar-inverse navbar-fixed-top">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <a class="navbar-brand" href="{{ path('index') }}">Backoffice</a>
            </div>
            <div id="navbar" class="collapse navbar-collapse">
                <ul class="nav navbar-nav">
                    <li><a href="{{ path('dashboard') }}">Dashboard</a></li>
                    <li class="active"><a href="{{ path('avisos') }}">Avisos</a></li>
                    <li><a href="{{ path('bloques') }}">Bloques</a></li>
                    <li><a href="{{ path('comunicaciones') }}">Comunicaciones</a></li>
                    <li><a href="{{ path('configuracion') }}">Configuración</a></li>
                    <li><a href="{{ path('fos_user_security_login') }}">Log in</a></li>
                </ul>
            </div><!--/.nav-collapse -->
        </div>
    </nav>
{% endblock %}

{% block content %}
    <div class="container">
    <div class="form-group">
        <input type="text" class="form-control" placeholder="Buscar aviso relacionado con...">
    </div>
    </div>

    <div class="row">
        <div class="col-md-10"></div>
        <div class="col-md-2">
            <a href="{{ path('avisos_new') }}">
            <button type="button" class="btn btn-primary btn-lg">Nuevo aviso</button>
            </a>
        </div>
    </div>


    {% for avis in avisos %}
    <div class="container">
        <table class="table table-bordered">
            <tbody>
            <tr>
                <td>Activo</td>
                <td><strong><a href="{{ path('avisos_show', {'id': avis.id}) }}"> {{ avis.nombre }} </a></strong>
                    <br>{{ avis.descripcion }}.
                </td>
                <td><a href="{{ path('avisos_edit', {'id':avis.id}) }}"> Configurar </a></td>
                <td>01/01/2015</td>
                <td>82 Intentos</td>
                <td>36 Conseguidos</td>
                <td>41 % CTR</td>
                <td><button type="button" class="btn btn-block"> V </button> </td>
            </tr>
            </tbody>
        </table>
    </div>
    {% endfor %}

{% endblock %}